.DS_Store
__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/

# Training artifacts — keep model weights & embeddings so Streamlit Cloud can load them
# best_model.pth      ← ~6 MB, needed at runtime, committed to repo
# raw_embeddings.pkl  ← ~786 KB, needed at runtime, committed to repo

# Jupyter
.ipynb_checkpoints/
